#include <vector>
#include <math.h>
#include <random>
#include <vector>
#include <array>
#include <algorithm>

#include <tuple>
#include <string>
#include <iostream> // cout, endl
#include <unordered_map>
#include <queue>

// Forward simulation function that takes initial configuration and a piecewise linear path
// as an input
#include "../include/planner.h"

for (int j = 0; j < 6; j++){
    planner_tree[0]->state[j] = 0;
}

std::vector<double> forward_simulator(
    std::vector<double> q_init,
    std::vector<std::vector<double>> piecewise_linear
){

    // Find the intersection points of the look

}

int main()
{
    std::unordered_map<int, leaf_node*> planner_tree;
    leaf_node* temp_ptr = new leaf_node;
    planner_tree.emplace(0, temp_ptr);
    std::cout << "Hello World";
}



//